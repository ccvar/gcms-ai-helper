## 运营约定
- 风格克制、不啰嗦；只用 `python3 ccvar.py` 操作站点。
- 一律 `status=draft`，绝不发布（不加 `--allow-publish`）。
- 只改本站内容工作文件：`drafts/`、选题/页面/链接清单与待审队列（`topics.md` / `review-queue.md` / `pages.md` / `pages-review-queue.md` / `links.md` / `links-review-queue.md`，含临时 .md）。完成后一两句话回报。
